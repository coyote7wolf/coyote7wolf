# mock profile 專用設定：關閉不需要的健康檢查避免 DOWN
management.health.redis.enabled=false
management.health.rabbit.enabled=false
management.health.db.enabled=false
# 額外可視需要加上 management.endpoints.web.exposure.include=health,info
server.port=8083
